1.【Git 提交规则】每完成一组边界清晰、可以独立验证的修改后，应优先运行与改动范围匹配的最小本地验证，然后及时提交、推送并查看远端 CI 结果，避免长期积累大量未提交文件。提交前必须先用 `git status --short` 和必要的 `git diff` 理解当前变更，排除 Gradle 缓存、构建产物、IDE 临时文件、无关子仓库状态等噪音；CI 失败时应优先修复，暂时无法修复时必须在最终回复中说明失败项、原因和后续补救命令。提交标题要简短、中文、能概括核心变更，禁止使用“提
  交当前变更”“更新代码”“WIP”等空泛标题；细节写在提交正文，不堆到标题里。提交后必须复查 `git log -1` 和 `git status
  --short`，标题不合格就用 `git commit --amend` 修正。
2.【改动前检查】修改代码前必须先查看相关文件、调用链和现有实现风格，避免凭空重写。若工作区已有未提交改动，禁止回滚或覆盖非本次任务相关内容；如果这些改动影响当前任务，应基于现状继续处理，只有在无法判断时才向用户确认。
3.【技术变更说明规则】只要本次任务修改了代码、资源、配置、脚本、数据库结构或其他非纯文档内容，就必须同步撰写或更新技术变更说明；只有完全没有触碰代码及相关工程文件、仅修改文档内容时，才可以不写技术变更说明。
撰写技术变更说明时要详细写出过往对话的所有修改内容，以方便后续问题排查
要在文档中包含一个标题（例如## 2025-10-03｜听歌记录前端接入
(TechChangeSpec_2025-10-03-Listen-Report-Integration)）、### 方案总览与实现要点、### 问题回溯与根因分析、### 受影响文件与关键点、### 行为变更明细、### 验收用例与标准、### 风险与回滚策略、### 版本信息与变更摘要（版本信息可以在app/build.gradle中读取）。；以上列举仅为必须要有的，可以按需添加其他内容。
4.【技术变更说明归档规则】后续每个相对独立的功能、修复、重构批次，原则上都在 `开发文档/技术变更说明/<年份>年<月份>/` 下新建单独技术变更说明文件，不要长期把多轮无关修改堆进同一份大文档。只有同一批次尚未提交、仍在补充同一主题时，才允许继续更新同一份文件；更新时必须直接在每个标题下分段添加内容，例如### 方案总览与实现要点、### 问题回溯与根因分析、### 受影响文件与关键点...等，不要在文档底部直接追加内容。
5.【分支隔离规则】如果用户不做要求，默认直接在main分支进行开发，如果用户要求了在独立分支进行开发，则要求如下：从最新 `main` 或 `origin/main` 新建独立 feature 分支；一个 feature 分支只承载一个边界清晰的功能或特性，以及该功能必需的测试和技术变更说明。开工前必须确认当前分支、基线提交和未提交改动，避免从其他 feature 分支继续派生导致无关提交混入。提交前必须复查 `git status --short`、`git diff` 以及相对 `main` 的新增提交范围，确保分支内只包含本次任务相关改动；不得把 main 之外的同一功能提交直接作为多个正式 feature 分支的共同新增提交。若需要把两个或多个功能一起编译、联调或演示，应新建临时 `integration/...`、`test/...` 或用户指定的集成分支，只在该集成分支合并多个功能分支，并明确它不是单功能 feature 分支。
